# AgentForge 服务启停命令

默认端口：
- 后端 API：`8000`
- 前端开发：`5173`

---

## 零、数据库一键初始化（首次部署）

确保 PostgreSQL 已安装 **pgvector** 扩展，并配置环境变量：

- `POSTGRESQL_USER` / `POSTGRESQL_PASSWORD`
- `PGHOST` / `PGPORT`（可选，默认 `localhost:5432`）
- `EMBEDDING_DIM`（可选，默认 `2048`）

在项目根目录执行：

```bash
# 建库 agentforge + agentforge_meta，并初始化表结构
.venv/bin/python scripts/init_databases.py

# 同上，并索引 RAG 知识库（需 QWEN_API_KEY）
.venv/bin/python scripts/init_databases.py --index-rag
```

脚本会自动完成：

1. 连接 `postgres` 管理库，创建 `agentforge`（向量库）与 `agentforge_meta`（关系库）
2. 执行 `sql/init_meta_schema.sql`（项目 / 会话 / 生成历史等表）
3. 执行 `sql/init_vector_schema.sql`（pgvector + `rag_chunks`，维度跟随 `EMBEDDING_DIM`）

若向量维度变更需重建表（会清空向量数据）：

```bash
.venv/bin/python scripts/init_databases.py --force-vector-recreate
```

后端启动时也会调用 `init_schema()` 做幂等补表，但**不会自动建库**，首次部署请先跑上述脚本。

---

## 一、启动

### 后端（FastAPI + Uvicorn）

在项目根目录执行：

```bash
bash scripts/run_api.sh
```

或手动启动：

```bash
cd ~/python/AgentForge
.venv/bin/uvicorn agentforge.api.main:app --host 127.0.0.1 --port 8000 --reload
```

访问：
- API 文档：http://127.0.0.1:8000/docs
- 健康检查：http://127.0.0.1:8000/api/health

### 前端（Vue 3 + Vite）

新开一个终端：

```bash
cd ~/python/AgentForge/frontend
npm run dev
```

访问：http://127.0.0.1:5173

对话编程多轮场景示例见：[对话编程场景示例.md](./对话编程场景示例.md)

---

## 二、停止 / 杀进程

### 按端口杀（推荐）

```bash
# 杀后端（8000）
lsof -ti :8000 | xargs kill -9

# 杀前端（5173）
lsof -ti :5173 | xargs kill -9
```

### 按进程名杀

```bash
# 杀 uvicorn
pkill -f "uvicorn agentforge.api.main:app"

# 杀 vite 开发服务
pkill -f "vite"
```

### 优雅停止

在启动服务的终端里按 `Ctrl + C` 即可。

---

## 三、查看运行状态

```bash
# 查看端口占用
lsof -i :8000
lsof -i :5173

# 快速探测后端是否存活
curl http://127.0.0.1:8000/api/health
```

---

## 四、验证脚本

在项目根目录执行：

```bash
# 模型 API
.venv/bin/python scripts/verify_models.py

# 向量库
.venv/bin/python scripts/verify_vector_db.py

# 关系型库
.venv/bin/python scripts/verify_meta_db.py

# Redis
.venv/bin/python scripts/verify_redis.py

# 后端 API（需先启动后端）
.venv/bin/python scripts/verify_api.py

# LangChain 沙盒
.venv/bin/python scripts/verify_sandbox.py

# 项目骨架（插件 + LangGraph）
.venv/bin/python scripts/verify_skeleton.py

# 三层多智能体（dry-run）
.venv/bin/python scripts/verify_agents.py

# Spring Boot 技术域 SubAgent
.venv/bin/python scripts/verify_spring_domains.py

# RAG 知识库（入库 + 检索 + rerank）
.venv/bin/python scripts/index_rag_corpus.py
.venv/bin/python scripts/verify_rag.py

# 三阶段代码生成
.venv/bin/python scripts/verify_codegen_stages.py

# 版本快照
.venv/bin/python scripts/verify_snapshots.py

# 上下文持久化
.venv/bin/python scripts/verify_context.py

# 端到端 CRUD
.venv/bin/python scripts/verify_e2e_crud.py

# 能力层模板（springdoc）
.venv/bin/python scripts/verify_capabilities.py

# 对话自动叠加能力层（Phase 2）
.venv/bin/python scripts/verify_capability_conversation.py

# LLM 造层（Phase 3，fixture）
.venv/bin/python scripts/verify_capability_generator.py
```

---

## 五、常见问题

### 端口已被占用（address already in use）

```bash
lsof -ti :8000 | xargs kill -9   # 或 5173
```

然后重新启动服务。

### 后端启动失败：环境变量未配置

确认已设置：
- `POSTGRESQL_USER` / `POSTGRESQL_PASSWORD`
- `QWEN_API_KEY` / `QWEN_BASE_URL`

### 前端无法连后端

1. 确认后端已在 `8000` 端口运行
2. 前端开发模式通过 Vite 代理 `/api` → `http://127.0.0.1:8000`，无需额外配置

---

## 六、推荐启动顺序

1. 确认 PostgreSQL、Redis 已运行
2. 启动后端：`bash scripts/run_api.sh`
3. 启动前端：`cd frontend && npm run dev`
4. 浏览器打开 http://127.0.0.1:5173

---

## 七、日志（loguru）

- 控制台：启动后端时直接输出
- 文件：`logs/agentforge.log`（10MB 轮转，保留 14 天）
- 日志级别：环境变量 `LOG_LEVEL`（默认 `INFO`）

```bash
# 实时查看日志
tail -f logs/agentforge.log

# 调试模式启动
LOG_LEVEL=DEBUG bash scripts/run_api.sh
```
